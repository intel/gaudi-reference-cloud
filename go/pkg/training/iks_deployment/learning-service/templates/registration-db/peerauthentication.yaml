# INTEL CONFIDENTIAL
# Copyright (C) 2023 Intel Corporation
apiVersion: security.istio.io/v1
kind: PeerAuthentication
metadata:
  name: registration-disable-mtls
  namespace: registration
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: registration-db-cluster
  mtls:
    # Connections to the Registration DB use TLS managed by CNPG
    mode: DISABLE
