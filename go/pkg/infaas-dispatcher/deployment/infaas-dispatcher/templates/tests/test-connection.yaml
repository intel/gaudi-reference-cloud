# INTEL CONFIDENTIAL
# Copyright (C) 2023 Intel Corporation
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infaas-dispatcher.fullname" . }}-test-connection"
  labels:
    {{- include "infaas-dispatcher.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infaas-dispatcher.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
