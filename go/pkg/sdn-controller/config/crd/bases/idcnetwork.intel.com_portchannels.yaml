---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: portchannels.idcnetwork.intel.com
spec:
  group: idcnetwork.intel.com
  names:
    kind: PortChannel
    listKind: PortChannelList
    plural: portchannels
    shortNames:
    - pc
    singular: portchannel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.labels.switch_fqdn
      name: Switch_FQDN
      type: string
    - jsonPath: .status.name
      name: Port_Channel
      type: string
    - jsonPath: .spec.vlanId
      name: Desired_VlanID
      type: string
    - jsonPath: .status.vlanId
      name: Last_Observed_VlanID
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PortChannel is the Schema for the portchannels API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: PortChannelSpec defines the desired state of PortChannel
            properties:
              bandwidth:
                type: integer
              description:
                type: string
              linkStatus:
                type: string
              mode:
                type: string
              name:
                type: string
              nativeVlan:
                format: int64
                type: integer
              trunkGroups:
                items:
                  type: string
                type: array
              untaggedVlan:
                format: int64
                type: integer
              vlanId:
                format: int64
                type: integer
            required:
            - mode
            - name
            type: object
          status:
            description: PortChannelStatus defines the observed state of PortChannel
            properties:
              bandwidth:
                type: integer
              description:
                type: string
              linkStatus:
                type: string
              mode:
                type: string
              name:
                type: string
              nativeVlan:
                format: int64
                type: integer
              trunkGroups:
                items:
                  type: string
                type: array
              untaggedVlan:
                format: int64
                type: integer
              vlanId:
                format: int64
                type: integer
            required:
            - mode
            - name
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
