- name: Make users passwordless for sudo in group {{ newuser }}
  become: true
  lineinfile:
    path: /etc/sudoers
    state: present
    regexp: '^%{{ newuser }}'
    line: '%{{ newuser }} ALL=(ALL) NOPASSWD: ALL'
    validate: 'visudo -cf %s'
