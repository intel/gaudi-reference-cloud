- name: Change SDP password
  user:
    name: sdp
    update_password: always
    password: "{{ devcloud_default_password|password_hash('sha512') }}"

- name: Change root password
  user:
    name: sdp
    update_password: always
    password: "{{ devcloud_default_password|password_hash('sha512') }}"
