apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "storage-metering-monitor.fullname" . }}-metrics-reader
  labels:
  {{- include "storage-metering-monitor.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
