apiVersion: v1
kind: Service
metadata:
  name: {{ include "storage-metering-monitor.fullname" . }}-metrics
  labels:
    control-plane: controller-manager
  {{- include "storage-metering-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    control-plane: controller-manager
  {{- include "storage-metering-monitor.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.metricsService.ports | toYaml | nindent 2 -}}
