apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "vm-instance-scheduler.fullname" . }}-metrics-reader
  labels:
  {{- include "vm-instance-scheduler.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get