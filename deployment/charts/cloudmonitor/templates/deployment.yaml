apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "idc-common.fullname" . }}
  namespace: {{ include "idc-common.namespace" . }}
  labels:
    {{- include "idc-common.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels: 
      {{- include "idc-common.selectorLabels" . | nindent 8 }}
  template:
    metadata:
      annotations:
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
        {{- include "idc-common.vaultAnnotations" . | nindent 8 }}
        {{- include "idc-common.vaultPkiAnnotations" . | nindent 8 }}
        {{- if .Values.otel.otelAnnotations }}
        {{- include "idc-common.otelAnnotations" . | nindent 8 }}
        {{- end }}
        {{- include "idc-common.globalDBSSLCerts" . | nindent 8 }}
        vault.hashicorp.com/agent-inject-secret-db_username: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-db_username: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.username }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-db_password: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-db_password: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.password }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-vmtoken: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-vmtoken: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.vmtoken }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-vmtokeniks: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-vmtokeniks: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.vmtokeniks }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-vmtokenbm: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-vmtokenbm: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.vmtokenbm }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-eksserverca: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-eksserverca: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.eksserverca }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-awsaccesskey: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-awsaccesskey: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.awsaccesskey }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-awsaccesskeysecret: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-awsaccesskeysecret: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.awsaccesskeysecret }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-otelconfig: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-otelconfig: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.otelconfig }}`}}
          {{`{{- end }}`}}
        vault.hashicorp.com/agent-inject-secret-rootca: "{{ .Values.database.vaultCredentialsPath }}"
        vault.hashicorp.com/agent-inject-template-rootca: |-
          {{`{{- with secret `}}"{{ .Values.database.vaultCredentialsPath }}"{{` -}}`}}
          {{`{{ .Data.data.rootca }}`}}
          {{`{{- end }}`}}
        {{- include "idc-common.vaultPkiAnnotations" . | nindent 8 }}
        vault.hashicorp.com/template-static-secret-render-interval: 2m
        # Add checksum to force deployment to restart pod if the configmap changes.
        checksum/configmap: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
      labels:
        {{- include "idc-common.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccount: {{ include "idc-common.serviceAccountName" . }}
      containers:
        - name: main
          image: {{ include "idc-common.image" . }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - "--config=/config.yaml"
            {{- include "idc-common.logArgs" . | nindent 12 }}
          env:
            {{- include "idc-common.proxyEnv" . | nindent 12 }}
            {{- include "idc-common.commonEnv" . | nindent 12 }}
            {{- include "idc-common.otelEnv" . | nindent 12 }}
          ports:
            - containerPort: 9090
              name: metrics
              protocol: TCP
            {{- include "idc-common.grpcPorts" . | nindent 12 }}
           # TODO: Include Liveness, Readiness and Startup probes
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
          - mountPath: /config.yaml
            name: config
            subPath: config.yaml
          - mountPath: /kubernetesfile
            name: demo
          securityContext:
            runAsNonRoot: true
            runAsUser: 65534
            runAsGroup: 65534
      volumes:
      - emptyDir:
          sizeLimit: 500Mi
        name: demo
      - configMap:
          name: {{ include "idc-common.fullname" . }}
        name: config
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
