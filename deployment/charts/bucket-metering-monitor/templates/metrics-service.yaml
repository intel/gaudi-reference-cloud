apiVersion: v1
kind: Service
metadata:
  name: {{ include "bucket-metering-monitor.fullname" . }}-metrics
  labels:
    control-plane: controller-manager
  {{- include "bucket-metering-monitor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    control-plane: controller-manager
  {{- include "bucket-metering-monitor.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.metricsService.ports | toYaml | nindent 2 -}}
