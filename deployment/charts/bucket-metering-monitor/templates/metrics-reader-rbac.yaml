apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "bucket-metering-monitor.fullname" . }}-metrics-reader
  labels:
  {{- include "bucket-metering-monitor.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
