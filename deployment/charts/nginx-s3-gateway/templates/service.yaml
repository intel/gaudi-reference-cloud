apiVersion: v1
kind: Service
metadata:
  name: "{{ $.Release.Name }}"
spec:
  ports:
  - name: nginx-s3
    port: 80
    targetPort: 80
    nodePort: 31969
    protocol: TCP
  selector:
    app: "{{ $.Release.Name }}"
  type: NodePort
