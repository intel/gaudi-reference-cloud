---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: nodegrouptopoolmappings.idcnetwork.intel.com
spec:
  group: idcnetwork.intel.com
  names:
    kind: NodeGroupToPoolMapping
    listKind: NodeGroupToPoolMappingList
    plural: nodegrouptopoolmappings
    shortNames:
    - mapping
    singular: nodegrouptopoolmapping
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.pool
      name: Pool
      type: string
    - jsonPath: .status.lastChangeTime
      name: Last_Change_Time
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeGroupToPoolMapping is the Schema for the nodegrouptopoolmappings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeGroupToPoolMappingSpec defines the desired state of NodeGroupToPoolMapping
            properties:
              pool:
                type: string
            type: object
          status:
            description: NodeGroupToPoolMappingStatus defines the observed state of
              NodeGroupToPoolMapping
            properties:
              lastChangeTime:
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
