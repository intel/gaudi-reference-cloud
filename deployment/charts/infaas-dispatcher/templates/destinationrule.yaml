apiVersion: networking.istio.io/v1
kind: DestinationRule
metadata:
  name: maas-destination-rule
spec:
  host: {{ include "infaas-dispatcher.fullname" . }}
  trafficPolicy:
    loadBalancer:
      simple: ROUND_ROBIN