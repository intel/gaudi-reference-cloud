# INTEL CONFIDENTIAL
# Copyright (C) 2023 Intel Corporation
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infaas-inference.fullname" . }}-test-connection"
  labels:
    {{- include "infaas-inference.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infaas-inference.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
