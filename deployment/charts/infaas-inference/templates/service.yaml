apiVersion: v1
kind: Service
metadata:
  name: {{ include "infaas-inference.fullname" . }}
  labels:
    {{- include "infaas-inference.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infaas-inference.selectorLabels" . | nindent 4 }}
