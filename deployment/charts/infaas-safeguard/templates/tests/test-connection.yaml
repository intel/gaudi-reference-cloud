apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "infaas-safeguard.fullname" . }}-test-connection"
  labels:
    {{- include "infaas-safeguard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "infaas-safeguard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never