apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "ssh-proxy-operator.fullname" . }}-metrics-reader
  labels:
  {{- include "ssh-proxy-operator.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get