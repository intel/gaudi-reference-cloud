apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-safeguard.fullname" . }}-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "inference-safeguard.fullname" . }}
