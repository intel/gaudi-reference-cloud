- name: Connect to BM instance created and run linux package update
  hosts: bm
  become: yes
  become_method: sudo

  tasks:
  - name: Update the linux base packages
    ansible.builtin.apt:
      update_cache: yes
      cache_valid_time: 60
      force_apt_get: true
